query MyGroups($moduleId: Int!) {
  myGroups(moduleId: $moduleId) {
    id
    name
    description
    requirements
    slug
    isMember
    isLeader
    module {
      id
      name
      code
    }
  }
}
